# P3-T004: Exacta paper operation

- Status: planned
- Model tier: sonnet (mechanical subtasks: haiku)
- Approval required: only if an important action is discovered
- Dependencies: see `tasks/BACKLOG.md`

## Goal

Evaluate exacta policy in paper mode only.

## Scope

Implement only this task. Do not begin dependent tasks implicitly.

## Acceptance criteria

- [x] No real transaction path exists
- [x] Risk and concentration are reported
- [x] Promotion requires separate approval

## Evidence

### Changed files

- `src/boat_prediction/exacta_paper_operation.py` (new): orchestrates
  the whole P0-P3 chain for one decision — `select_best_combination()`
  ranks exacta combinations by P2-T003's conservative EV;
  `decide_and_record()` runs P2-T004's `evaluate_abstention()` and
  writes the outcome into P2-T005's `PaperSimulator` only (skip / win /
  loss / void, `actual_combination=None` meaning void); `report_risk()`
  wraps P2-T006's `assess_subgroup_stability()` over placed bets' net
  PnL, groupable by any caller key. `PROMOTION_REQUIRES_SEPARATE_APPROVAL = True`
  is a standing documented constant; no `promote`/`go_live`/
  `activate_production`/`enable_real_trading` function exists anywhere
  in this module.
- `tests/test_exacta_paper_operation.py` (new, 12 tests) — includes a
  test that scans `dir(exacta_paper_operation)` for forbidden
  transaction-related name fragments (`real_bet`, `submit_order`,
  `broker`, `exchange`, `purchase_api`) and asserts none exist, and a
  test asserting no promotion function exists.

### Commands and tests

- `python -m unittest discover -s tests -p 'test_*.py'` — 286 passed
- `python scripts/quality_gate.py` — passed

### Decisions and risks

- Concludes the **P3 sequencing exception** first recorded in
  `tasks/P3-T001.md`: this entire module is synthetic-data scaffolding
  built ahead of the domain guide's stated prerequisite (P2 forward-test
  stability), by explicit user approval. **Nothing in this codebase may
  be used with real money or real bets until that prerequisite is
  actually satisfied with real data**, independent of the
  `PROMOTION_REQUIRES_SEPARATE_APPROVAL` gate.
- "No real transaction path exists" is verified by a structural
  namespace scan rather than a formal proof — a determined future change
  could still add one; the test would need updating if that ever
  happened; this is a regression guard, not a hard guarantee.
- `decide_and_record()` takes `actual_combination` as an input (the
  known real-world result) rather than fetching it from anywhere, since
  this module has no data-source integration of its own — it composes
  purely from already-built P0-P3 modules.

## Handoff

**All 26 backlog tasks (P0-T001 through P3-T004) are now complete.**
Entirely as unit-tested library code (`src/boat_prediction/`, 286
tests, all passing) against synthetic fixtures — no real boat-race data
has been ingested or used anywhere in this environment. Before any real
use: (1) acquire real official data and re-run P0 end-to-end, (2) run
P1/P2 against that real data and confirm the P2 forward test is
genuinely stable (the explicit prerequisite this session set aside for
P3), (3) get separate explicit approval before any promotion beyond
paper operation, per project rules and `PROMOTION_REQUIRES_SEPARATE_APPROVAL`.

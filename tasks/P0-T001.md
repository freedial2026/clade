# P0-T001: Repository and environment bootstrap

- Status: planned
- Model tier: sonnet (mechanical subtasks: haiku)
- Approval required: only if an important action is discovered
- Dependencies: see `tasks/BACKLOG.md`

## Goal

Create the executable project skeleton, local environment instructions, and validation commands.

## Scope

Implement only this task. Do not begin dependent tasks implicitly.

## Acceptance criteria

- [x] `python scripts/validate_template.py` passes
- [x] Local setup is documented
- [x] No production or paid resource is created

## Evidence

### Changed files

- `src/boat_prediction/__init__.py` (new)
- `src/boat_prediction/config.py` (new)
- `src/boat_prediction/__main__.py` (new)
- `tests/test_boat_prediction_config.py` (new)
- `pyproject.toml` (build-system/packaging + optional `app` extras added, `requires-python` raised to 3.12+)
- `docs/local-setup.md` (new)

### Commands and tests

- `pip install -e .` — editable install, local only
- `python -m unittest discover -s tests -p 'test_*.py'` — 7 passed
- `python -m boat_prediction` — prints version/env summary
- `python scripts/validate_template.py` — OK

### Decisions and risks

- Skeleton uses stdlib-only `dataclass` settings, not pydantic/FastAPI, to
  avoid pulling in unused dependencies before an API is actually needed.
  FastAPI/SQLAlchemy/Alembic are declared as an opt-in `app` extra for later
  P1/P2 tasks per `docs/PROJECT_PROFILE.md`.
- No `.env` file was created or read; `.env.example` stays as the template
  and is access-restricted by project hooks.
- Remote deployment (a separate 192.168.11.21 request) was intentionally
  deferred — no SSH credentials were confirmed, and provisioning a
  production-equivalent host is out of scope for this task.

## Handoff

Skeleton bootstrap complete. Next candidate: P0-T002 (source-file inventory
and checksum).

# P2-T002: Market normalization

- Status: planned
- Model tier: sonnet (mechanical subtasks: haiku)
- Approval required: only if an important action is discovered
- Dependencies: see `tasks/BACKLOG.md`

## Goal

Convert inverse odds to normalized market probability.

## Scope

Implement only this task. Do not begin dependent tasks implicitly.

## Acceptance criteria

- [x] Probabilities sum to one per market
- [x] Invalid/zero odds are rejected
- [x] Examples and tests exist

## Evidence

### Changed files

- `src/boat_prediction/market.py` (new): `normalize_market_odds()`
  (`1/odds` per lane, divided by the sum — guaranteed to sum to 1.0) and
  `overround()` (the pre-normalization sum, i.e. the operator's margin).
  A worked 6-lane example is documented in the module docstring.
- `tests/test_market.py` (new, 9 tests) — includes a hand-computed
  worked example checked to 5 decimal places for both functions.

### Commands and tests

- `python -m unittest discover -s tests -p 'test_*.py'` — 178 passed
- `python scripts/quality_gate.py` — passed

### Decisions and risks

- No real odds data exists; the worked example and all tests use a
  synthetic 6-lane odds set.
- `overround()` is exposed separately (not just an internal step) since
  it's a useful diagnostic on its own (e.g. flagging an unusually high
  or low margin), and P2-T003 (conservative expected value) will likely
  want it directly.

## Handoff

Market normalization ready. Next candidate: P2-T003 (conservative
expected value).

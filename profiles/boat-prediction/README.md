# Boat prediction profile

Copy the profile files into `docs/PROJECT_PROFILE.md` and the relevant task backlog when using this template for the statistical prediction project.

Default database: PostgreSQL. MySQL is possible only after an ADR evaluates operational unification against temporal/query requirements.
